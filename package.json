{
  "name": "chicken-meeting-backend",
  "version": "2.0.0-cloud",
  "private": true,
  "description": "基于腾讯会议 Remote MCP 的多人会议预约网站（Cloudflare Worker + D1）",
  "type": "module",
  "main": "backend/server.js",
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check backend/server.js && node --check backend/services/tencentMeeting.js && node --check backend/database/database.js && node --check backend/utils/auth.js && node --check backend/utils/http.js && node --check backend/utils/rateLimit.js && node --check backend/utils/meetingView.js && node --check backend/utils/env.js && node --check backend/routes/meetings.js && node --check backend/routes/system.js && node --check backend/routes/settings.js && node --check backend/routes/auth.js",
    "db:init": "wrangler d1 execute chicken_meeting_db --remote --file=schema.sql"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "luxon": "2.5.2"
  },
  "devDependencies": {
    "wrangler": "^4.136.1"
  }
}
